import Link from "next/link";

export default function BottomNav() {
  return (
    <nav className="fixed bottom-0 left-0 right-0 border-t bg-white p-4 shadow">
      <div className="mx-auto flex max-w-md justify-around">
        /dashboardHome</Link>

        /studyStudy</Link>

        decks">Decks</Link>

        /cardsCards</Link>
      </div>
    </nav>
  );
}